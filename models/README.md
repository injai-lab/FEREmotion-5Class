# Local model folder

Put local-only model checkpoints here while training or exporting.

Expected checkpoint name for the default ONNX export command:

```text
facenet_5class_best.pth
```

Files in this folder are ignored by git except this README.
